/*
 * This file intentionally carries no SPDX-License-Identifier or
 * SPDX-FileCopyrightText tag. A vendored copy of an upstream project often has
 * none, so its license and copyright are declared by the hosting module's
 * REUSE.toml. 'west spdx' must still honour that once the file is attributed to
 * the bundled component rather than to the module.
 */

#include <vendored.h>

int sbom_vendored_from_reuse_toml(void)
{
	return 17;
}
