# LVGL configuration for native_sim
CONFIG_LV_COLOR_DEPTH_32=y
CONFIG_GPIO=y
CONFIG_INPUT=y

# Disable network features that won't work in native_sim
CONFIG_NET_DHCPV4=n
CONFIG_NET_CONFIG_SETTINGS=n

# Use loopback for testing
CONFIG_NET_LOOPBACK=y
