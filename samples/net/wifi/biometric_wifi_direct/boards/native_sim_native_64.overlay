/*
 * SPDX-License-Identifier: Apache-2.0
 */

/* Same as native_sim.overlay — biometrics emulator for 64-bit native_sim. */

/ {
	aliases {
		fingerprint = &biometrics_emul;
	};

	biometrics_emul: biometrics-emul {
		compatible = "zephyr,biometrics-emul";
		status = "okay";
	};
};
